{% extends "users/base.twig" %}

{% block content %}
    <form method="POST" action="/register">
        <div class="card shadow-sm">
            <div class="card-header">
                <strong>Register</strong>
            </div>

            <div class="card-body">
                {% if errors.fullName %}<div>{{ errors.fullName }}</div>{% endif %}
                {% if errors.username %}<div>{{ errors.username }}</div>{% endif %}
                {% if errors.usernameExists %}<div>{{ errors.usernameExists }}</div>{% endif %}
                {% if errors.password %}<div>{{ errors.password }}</div>{% endif %}
                <div class="row mb-3">
                    <div class="col-sm-3 fw-bold"><label for="full-name">Full Name</label></div>
                    <div class="col-sm-9"><input type="text" id="full-name" name="full-name" class="form-control"></div>
                </div>
                <div class="row mb-3">
                    <div class="col-sm-3 fw-bold"><label for="username">Username</label></div>
                    <div class="col-sm-9"><input type="text" id="username" name="username" class="form-control"></div>
                </div>
                <div class="row mb-3">
                    <div class="col-sm-3 fw-bold"><label for="password">Password</label></div>
                    <div class="col-sm-9"><input type="password" id="password" name="password" class="form-control"></div>
                </div>
            </div>

            <div class="card-footer d-flex gap-2">
                <input type="submit" value="Register" class="btn btn-primary">
            </div>
        </div>
    </form>
{% endblock %}